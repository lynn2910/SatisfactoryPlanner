import { Factory } from './create_factory.js'
import { Processor } from './builder'

/**
 * Draw create_factory on the canvas
 * @param {Processor} proc
 * @param {Factory} factory
 */
function draw_factory(proc, factory) {

}